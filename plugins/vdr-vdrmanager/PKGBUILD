# Maintainer: Christopher Reimer <vdr4arch[at]creimer[dot]net>
pkgname=vdr-vdrmanager
pkgver=0.9
_vdrapi=2.0.0
pkgrel=1
pkgdesc="Allows remote programming VDR using VDR-Manager running on Android devices"
url="http://projects.vdr-developer.org/projects/vdr-manager"
arch=('x86_64' 'i686')
license=('GPL2')
depends=('gcc-libs' "vdr-api=${_vdrapi}")
_plugname=$(echo $pkgname | sed 's/vdr-//g')
source=("http://projects.vdr-developer.org/attachments/download/1313/$pkgname-$pkgver.tar.gz")
md5sums=('132ffc03eefbe5d3fb708158b254e02e')

build() {
  #mkdir "${srcdir}/${_plugname}-${pkgver}"
  cd "${srcdir}/vdr-${_plugname}-${pkgver}"
  make GITTAG=''
}

package() {
  cd "${srcdir}/${pkgname}-${pkgver}"

  make DESTDIR="$pkgdir" install
}
